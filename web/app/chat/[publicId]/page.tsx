import { memo } from "react";

interface IPagePropTypes {}

const ChatDetailsPage = ({}: IPagePropTypes) => {
  return <div className="">chat details page</div>;
};

export default memo(ChatDetailsPage);
